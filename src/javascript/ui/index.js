import initBtnPlay from './btn-play';
import initVolumeSlider from './volume-slider';
import initProgressBar from './progress-bar';

export default function initUI($) {

  initBtnPlay($);
  initVolumeSlider($);
  initProgressBar($);

}
